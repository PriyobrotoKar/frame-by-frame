{
  "name": "frame-by-frame",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "prepare": "husky",
    "lint": "turbo lint",
    "lint:api": "turbo lint --filter=api",
    "format": "turbo format",
    "format:check:api": "pnpm run --filter=api format:check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "turbo": "^2.4.4",
    "typescript": "^5"
  },
  "packageManager": "pnpm@9.5.0"
}
